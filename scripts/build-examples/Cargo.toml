[package]
name = "build-examples"
description = "Runs `forc build` and `forc fmt --check` on Sway projects specified by path or within Sway `examples` directory."
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
anyhow = "1.0.41"
clap = { version = "3.1", features = ["env", "derive"] }
