[project]
authors = ["Fuel Labs <contact@fuel.sh>"]
entry = "main.sw"
license = "Apache-2.0"
name = "registers"

[dependencies]
core = { git = "http://github.com/FuelLabs/sway-lib-core" }
std = { path = "../../../" }
