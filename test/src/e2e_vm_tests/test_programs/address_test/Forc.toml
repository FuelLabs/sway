[project]
authors = ["Fuel Labs <contact@fuel.sh>"]
entry = "main.sw"
license = "Apache-2.0"
name = "address_test"

[dependencies]
std = { git = "http://github.com/FuelLabs/sway-lib-std", branch = "master" }
