script;
// if test passes, return true

use std::types::B512;
use std::types::build_from_b256s;
use std::ecr::ec_recover;

fn main() -> bool {
    // https://www.royalfork.org/2017/12/10/eth-graphical-address/?ref=hackernoon.com



    // // get the address: SHA256(pub_key)
    // let address: b256 = ;

    // // define a message
    // let message = "Hello from fuel. The comitted number is 42.";

    // // create a signature
    // // let signature =
    // // hash the message (SHA256(message))
    // let msg_hash = ;

    // // recover the address
    // let mut recovered_address: b256 = 0x;

    true
}
ln -s /home/furnic/dev/fuel/fuel-core-master ~/bin