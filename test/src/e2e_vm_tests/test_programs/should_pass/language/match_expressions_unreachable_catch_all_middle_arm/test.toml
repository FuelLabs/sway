category = "compile"

#check: $()Catch-all match arm makes arms below it unreachable. Match arms below catch-all arm "_" will never be matched.

#check: $()let _x = match e1_1 {
#nextln: $()This is the value to match on.

#check: $()_ => 0,
#nextln: $()This catch-all match arm makes all arms below it unreachable.
#check: $()Enum::E(_) => 0,
#nextln: $()These arms, coming after "_", will never be matched.

#check: $()Catch-all match arm makes arms below it unreachable. Match arms below catch-all arm "_" will never be matched.

#check: $()let _x = match e1_2 {
#nextln: $()This is the value to match on.

#check: $()_ => 0,
#nextln: $()This catch-all match arm makes all arms below it unreachable.
#check: $()_ => 0,
#nextln: $()These arms, coming after "_", will never be matched.

#check: $()Catch-all match arm makes arms below it unreachable. Match arms below catch-all arm "x" will never be matched.

#check: $()let _x = match e2_1 {
#nextln: $()This is the value to match on.

#check: $()x => {
#nextln: $()This catch-all match arm makes all arms below it unreachable.
#check: $()Enum::E(_) => 0,
#nextln: $()These arms, coming after "x", will never be matched.

#check: $()Catch-all match arm makes arms below it unreachable. Match arms below catch-all arm "x" will never be matched.

#check: $()let _x = match e2_2 {
#nextln: $()This is the value to match on.

#check: $()x => {
#nextln: $()This catch-all match arm makes all arms below it unreachable.
#check: $()_ => 0,
#nextln: $()These arms, coming after "x", will never be matched.

#check: $()Catch-all match arm makes arms below it unreachable. Match arms below catch-all arm "_" will never be matched.

#check: $()let _x = match s1 {
#nextln: $()This is the value to match on.

#check: $()_ => 0,
#nextln: $()This catch-all match arm makes all arms below it unreachable.
#check: $()_ => 0,
#nextln: $()These arms, coming after "_", will never be matched.

#check: $()Catch-all match arm makes arms below it unreachable. Match arms below catch-all arm "x" will never be matched.

#check: $()let _x = match s2 {
#nextln: $()This is the value to match on.

#check: $()x => {
#nextln: $()This catch-all match arm makes all arms below it unreachable.
#check: $()_ => 0,
#nextln: $()These arms, coming after "x", will never be matched.

#check: $()Catch-all match arm makes arms below it unreachable. Match arms below catch-all arm "Struct { x, y, z }" will never be matched.

#check: $()let _x = match s3 {
#nextln: $()This is the value to match on.

#check: $()Struct { x, y, z } => if x { y } else { z.0 },
#nextln: $()This catch-all match arm makes all arms below it unreachable.
#check: $()_ => 0,
#nextln: $()These arms, coming after "Struct { x, y, z }", will never be matched.

#check: $()Catch-all match arm makes arms below it unreachable. Match arms below catch-all arm "Struct { x:_, y:_, z:_ }" will never be matched.

#check: $()let _x = match s4 {
#nextln: $()This is the value to match on.

#check: $()Struct { x:_, y:_, z:_ } => 0,
#nextln: $()This catch-all match arm makes all arms below it unreachable.
#check: $()_ => 0,
#nextln: $()These arms, coming after "Struct { x:_, y:_, z:_ }", will never be matched.

#check: $()Catch-all match arm makes arms below it unreachable. Match arms below catch-all arm "Struct { x: a, y: b, z: c }" will never be matched.

#check: $()let _x = match s5 {
#nextln: $()This is the value to match on.

#check: $()Struct { x: a, y: b, z: c } => if a { b } else { c.0 },
#nextln: $()This catch-all match arm makes all arms below it unreachable.
#check: $()_ => 0,
#nextln: $()These arms, coming after "Struct { x: a, y: b, z: c }", will never be matched.

#check: $()Catch-all match arm makes arms below it unreachable. Match arms below catch-all arm "Struct { x: a, y: b, z: (j, k, l) }" will never be matched.

#check: $()let _x = match s6 {
#nextln: $()This is the value to match on.

#check: $()Struct { x: a, y: b, z: (j, k, l) } => if a { b } else { j + k + l },
#nextln: $()This catch-all match arm makes all arms below it unreachable.
#check: $()_ => 0,
#nextln: $()These arms, coming after "Struct { x: a, y: b, z: (j, k, l) }", will never be matched.

#check: $()Catch-all match arm makes arms below it unreachable. Match arms below catch-all arm "Struct { x: a, y: b, z: (_, _, _) }" will never be matched.

#check: $()let _x = match s7 {
#nextln: $()This is the value to match on.

#check: $()Struct { x: a, y: b, z: (_, _, _) } => if a { b } else { 0 },
#nextln: $()This catch-all match arm makes all arms below it unreachable.
#check: $()_ => 0,
#nextln: $()These arms, coming after "Struct { x: a, y: b, z: (_, _, _) }", will never be matched.

#check: $()Catch-all match arm makes arms below it unreachable. Match arms below catch-all arm "_" will never be matched.

#check: $()let _x = match t1 {
#nextln: $()This is the value to match on.

#check: $()_ => 0,
#nextln: $()This catch-all match arm makes all arms below it unreachable.
#check: $()_ => 0,
#nextln: $()These arms, coming after "_", will never be matched.

#check: $()Catch-all match arm makes arms below it unreachable. Match arms below catch-all arm "x" will never be matched.

#check: $()let _x = match t2 {
#nextln: $()This is the value to match on.

#check: $()x => x.3,
#nextln: $()This catch-all match arm makes all arms below it unreachable.
#check: $()_ => 0,
#nextln: $()These arms, coming after "x", will never be matched.

#check: $()Catch-all match arm makes arms below it unreachable. Match arms below catch-all arm "(b, e, s, n)" will never be matched.

#check: $()let _x = match t3 {
#nextln: $()This is the value to match on.

#check: $()(b, e, s, n) => {
#nextln: $()This catch-all match arm makes all arms below it unreachable.
#check: $()_ => 0,
#nextln: $()These arms, coming after "(b, e, s, n)", will never be matched.

#check: $()Catch-all match arm makes arms below it unreachable. Match arms below catch-all arm "(_, _, _, _)" will never be matched.

#check: $()let _x = match t4 {
#nextln: $()This is the value to match on.

#check: $()(_, _, _, _) => 0,
#nextln: $()This catch-all match arm makes all arms below it unreachable.
#check: $()_ => 0,
#nextln: $()These arms, coming after "(_, _, _, _)", will never be matched.

#check: $()Catch-all match arm makes arms below it unreachable. Match arms below catch-all arm "(_, _, _, n)" will never be matched.

#check: $()let _x = match t5 {
#nextln: $()This is the value to match on.

#check: $()(_, _, _, n) => n,
#nextln: $()This catch-all match arm makes all arms below it unreachable.
#check: $()_ => 0,
#nextln: $()These arms, coming after "(_, _, _, n)", will never be matched.

#check: $()Catch-all match arm makes arms below it unreachable. Match arms below catch-all arm "(_, _, Struct { x, y, z: (j , k, l)}, n)" will never be matched.

#check: $()let _x = match t6 {
#nextln: $()This is the value to match on.

#check: $()(_, _, Struct { x, y, z: (j , k, l)}, n) => {
#nextln: $()This catch-all match arm makes all arms below it unreachable.
#check: $()_ => 0,
#nextln: $()These arms, coming after "(_, _, Struct { x, y, z: (j , k, l)}, n)", will never be matched.

#check: $()Catch-all match arm makes arms below it unreachable. Match arms below catch-all arm "(_, _, Struct { x: _, y: _, z: (_ , k, _)}, n)" will never be matched.

#check: $()let _x = match t7 {
#nextln: $()This is the value to match on.

#check: $()(_, _, Struct { x: _, y: _, z: (_ , k, _)}, n) => {
#nextln: $()This catch-all match arm makes all arms below it unreachable.
#check: $()_ => 0,
#nextln: $()These arms, coming after "(_, _, Struct { x: _, y: _, z: (_ , k, _)}, n)", will never be matched.

expected_warnings = 18