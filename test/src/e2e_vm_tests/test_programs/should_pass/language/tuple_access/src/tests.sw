library;

use ::utils::*;

pub fn easy_test() -> u64 {
    // let (_foo, _bar) = gimme_a_pair();
    // let (_x, _y): (u32, bool) = (10, true);r
    // let (a, (b, (c, d) ) ): (u64, (u32, (bool, str[2]) ) ) = (42u64, (42u32, (true, "ok") ) );
    // let (e, f) = gimme_one(10u64);

    test(true, false);
    test (42, 42);

    // a
    42u64
}
