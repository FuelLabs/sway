[project]
authors = ["Fuel Labs <contact@fuel.sh>"]
license = "Apache-2.0"
name = "asm_expr_basic"
entry = "main.sw"

[dependencies]
core = { git = "https://github.com/FuelLabs/sway-lib-core", branch = "master" }
