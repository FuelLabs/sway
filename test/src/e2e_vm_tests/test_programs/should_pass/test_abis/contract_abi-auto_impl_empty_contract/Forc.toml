[project]
# BEWARE: The project name intentionally contains `-`, in order to test
#         correct generation of the ABI name.
name = "contract_abi-auto_impl_empty_contract"
authors = ["Fuel Labs <contact@fuel.sh>"]
entry = "main.sw"
license = "Apache-2.0"

[dependencies]
std = { path = "../../../../reduced_std_libs/sway-lib-std-core" }
