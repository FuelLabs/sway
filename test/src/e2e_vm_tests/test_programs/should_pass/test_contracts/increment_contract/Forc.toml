[project]
authors = ["Fuel Labs <contact@fuel.sh>"]
entry = "main.sw"
license = "Apache-2.0"
name = "increment_contract"

[dependencies]
increment_abi = { path = "../../test_abis/increment_abi" }
std = { git = "https://github.com/FuelLabs/sway-lib-std", branch = "master" }
