cmds = [
    "forc test --path {root} --release --experimental const_generics --logs --ir final",
    # { repeat = "for-each-block", cmds = [
    #     "forc test --path {root} --release --experimental const_generics"
    # ] }
]