[project]
name = "ecall_basic"
authors = ["Fuel Labs <contact@fuel.sh>"]
entry = "main.sw"
implicit-std = false
license = "Apache-2.0"

[dependencies]
core = { path = "../../../../../../../sway-lib-core" }
