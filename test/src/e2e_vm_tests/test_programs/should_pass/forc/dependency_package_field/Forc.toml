[project]
authors = ["Fuel Labs <contact@fuel.sh>"]
entry = "main.sw"
license = "Apache-2.0"
name = "dependency_package_field"

[dependencies]
# Use `std` package as `std_alt` to test `package` field.
std-alt = { path = "../../../../reduced_std_libs/sway-lib-std-assert", package = "std" }
