[project]
authors = ["Fuel Labs <contact@fuel.sh>"]
entry = "main.sw"
implicit-std = false
license = "Apache-2.0"
name = "dependency_package_field"

[dependencies]
# Use `utils` package as `utils_alt` to test `package` field.
std = { path = "../../../../reduced_std_libs/sway-lib-std-core" }
utils-alt = { path = "../../../../../e2e_vm_tests/utils", package = "utils" }
