category = "fail"

# check: $()for (_n,_m) in vector.iter()
# nextln: $()No method "unwrap(Option<u64>) -> (_, _)" found for type "Option<u64>".
# nextln: $()Matching method:
# nextln: $()unwrap(Option<u64>) -> u64 in Option<u64>
