[project]
authors = ["Fuel Labs <contact@fuel.sh>"]
entry = "main.sw"
license = "Apache-2.0"
name = "array_oob_variable_index"

[dependencies]
std = { path = "../../../reduced_std_libs/sway-lib-std-assert" }
