category = "fail"

# check: let b = foo::bar::ExampleStruct::<bool> { a_field: 5u64 };
# nextln: $()Mismatched types.
# nextln: $()expected: bool
# nextln: $()found:    u64.
# nextln: $()help: Struct field's type must match the type specified in its declaration.
