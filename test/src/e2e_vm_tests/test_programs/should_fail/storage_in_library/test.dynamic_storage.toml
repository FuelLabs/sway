category = "fail"
experimental = { new_encoding = true, dynamic_storage = true }

# Skip the first warning.
# check: storage {

# check: storage {
# nextln: item: u64 = 0,
# nextln: }
# nextln: $()Declaring storage in a library is not allowed.
