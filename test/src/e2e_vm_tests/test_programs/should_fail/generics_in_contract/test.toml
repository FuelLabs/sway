category = "fail"

# check: $()error
# nextln: generics_in_contract/src/main.sw:19:17
# check: $()vec.push(item);
# check: $()No method "push(Vec<V>, K)" found for type "Vec<V>".
# nextln: $()Matching method:
# nextln: $()push(Vec<V>, V) -> () in Vec<V>

# check: $()error
# nextln: generics_in_contract/src/main.sw:39:9
# check: $()vec
# nextln: $()Mismatched types.
# nextln: $()expected: Vec<V>
# nextln: $()found:    Vec<K>
# nextln: $()Implicit return must match up with block's type.
