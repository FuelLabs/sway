category = "fail"

# check: $()error
# check: enum_bad_type_inference/src/main.sw:25:9
# check: $()y = x;
# nextln: $()Mismatched types.
# nextln: $()expected: enum_bad_type_inference::Result<bool, enum_bad_type_inference::SaleError>
# nextln: $()found:    enum_bad_type_inference::Result<u64, enum_bad_type_inference::SaleError>.
