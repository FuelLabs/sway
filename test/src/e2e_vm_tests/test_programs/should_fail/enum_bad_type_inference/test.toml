category = "fail"

# check: $()error
# check: enum_bad_type_inference/src/main.sw:25:9
# check: $()y = x;
# nextln: $()Mismatched types.
# nextln: $()expected: Result<bool, SaleError>
# nextln: $()found:    Result<u64, SaleError>
