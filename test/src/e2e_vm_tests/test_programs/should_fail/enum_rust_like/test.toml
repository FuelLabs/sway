category = "fail"

# check: $()error
# check: enum_rust_like/src/main.sw:7:5
# check: $()Ok,    // Illegal
# nextln: $() Expected ':'. Enum variants must be on the form 'Foo : ()' or 'Bar : u32'.

# check: $()error
# check: enum_rust_like/src/main.sw:12:5
# check: $()F(u32),        // Illegal
# nextln: $() Expected ':'. Enum variants must be on the form 'Foo : ()' or 'Bar : u32'.
