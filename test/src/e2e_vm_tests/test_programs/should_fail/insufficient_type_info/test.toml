category = "fail"

# check: Option::None::<T>;
# nextln: $()This returns a value of type Option<T>, which is not assigned to anything and is ignored.

# check: Option::None::<T>;
# nextln: $()Could not find symbol "T" in this scope.

# check: foo()
# nextln: $()Cannot infer type for type parameter "T". Insufficient type information provided. Try annotating its type.