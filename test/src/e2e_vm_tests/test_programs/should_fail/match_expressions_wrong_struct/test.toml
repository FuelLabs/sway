category = "fail"

# check: Point { x: 3, y } => { y },
# nextln: $()Mismatched types.
# nextln: $()expected: {numeric}
# nextln: $()found:    match_expressions_wrong_struct::Point.

# check: Point { x: 3, y: 4 } => { 24 },

# check: Point { x: 3, y: 4 } => { 24 },
# nextln: $()Mismatched types.
# nextln: $()expected: {numeric}
# nextln: $()found:    match_expressions_wrong_struct::Point.

# check: Data { value: 1u64 } => { false },
# nextln: $()Mismatched types.
# nextln: $()expected: bool
# nextln: $()found:    u64.

# check: Bar(x) => x,
# nextln: $()Could not find symbol "Bar" in this scope.