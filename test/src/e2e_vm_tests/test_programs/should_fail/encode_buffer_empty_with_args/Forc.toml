[project]
authors = ["Fuel Labs <contact@fuel.sh>"]
license = "Apache-2.0"
name = "encode_buffer_empty_with_args"
entry = "main.sw"

[dependencies]
core = { path = "../../../../../../sway-lib-core" }
