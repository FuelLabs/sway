category = "fail"

#check: $()const CONST_INVALID_TYPE: u64 = "not a u64";
#nextln: $()Mismatched types.
#nextln: $()expected: u64
#nextln: $()found:    str
#nextln: $()This declaration's type annotation does not match up with the assigned expression's type.
