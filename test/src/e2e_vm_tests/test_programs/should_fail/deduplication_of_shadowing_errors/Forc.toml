[project]
authors = ["Fuel Labs <contact@fuel.sh>"]
entry = "main.sw"
license = "Apache-2.0"
name = "deduplication_of_shadowing_errors"

[dependencies]
std = { path = "../../../../../../sway-lib-std" }
