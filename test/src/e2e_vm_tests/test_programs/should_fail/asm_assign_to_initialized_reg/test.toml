category = "fail"

# check: $()asm(r1: 0, r2: 0, r3, r4) {
# nextln: $()Register "r1" is initialized but later assigned which is not allowed. Consider uninitializing "r1".

# check: $()asm(r1: 0, r2: 0, r3, r4) {
# nextln: $()Register "r2" is initialized but later assigned which is not allowed. Consider uninitializing "r2".
