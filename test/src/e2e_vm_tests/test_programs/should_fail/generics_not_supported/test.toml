category = "fail"

# check: $()let _b = lib_a::inner_lib::<u64>::func();
# nextln: $()generics are not supported here

# check: $()let _c = lib_a::inner_lib::C::<u32>;
# nextln: $()generics are not supported here

# check: $()let _c = lib_a::inner_lib::<u32>::C;
# nextln: $()generics are not supported here

# check: $()let _s = lib_a::inner_lib::<u32>::S2 {};
# nextln: $()"S2" does not take type arguments as prefix.
