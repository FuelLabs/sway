category = "fail"

# check: $()let b = lib_a::inner_lib::<u64>::func();
# nextln: $()generics are not supported here

# check: $()let c = lib_a::inner_lib::C::<u32>;
# nextln: $()generics are not supported here

# check: $()let c = lib_a::inner_lib::<u32>::C;
# nextln: $()generics are not supported here