script {
    entry fn main() -> u64 {
        entry():

        c0 = const u64 0

        v0 = asm() -> () {
        }

        v1 = asm() -> () zero {
        }

        v2 = asm() -> u64 zero {
        }

        rv = const u64 0
        ret u64 rv
    }
}
